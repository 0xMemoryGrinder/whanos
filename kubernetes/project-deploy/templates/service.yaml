apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-deploy.fullname" . }}
  labels:
    {{- include "project-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project-deploy.selectorLabels" . | nindent 4 }}
